services:
  postgres:
    image: postgres:16
    container_name: postgres-db-convocation
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: cd
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: db-convocation
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: